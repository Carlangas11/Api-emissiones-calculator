export enum ESourceData {
    excel = 'excel',
    integration = 'integration',
    webForm = 'form',
}

export enum EStatusData {
    processing = 'Processing',
    completed = 'Completed',
    error = 'Failed',
}
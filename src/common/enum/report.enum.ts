export enum SourceData {
    excel = 'excel',
    integration = 'integration',
    webForm = 'form',
}

export enum StatusData {
    processing = 'Processing',
    completed = 'Completed',
    error = 'Failed',
}